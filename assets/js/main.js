define([
    'views/AppView'
], function(AppView) {
    'use strict';

    // Wait until the DOM is ready
    $(function() {
        // Initialise the app and get everything going
        var app = new AppView();
    });
});