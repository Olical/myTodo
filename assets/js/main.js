define([
    'views/AppView'
], function(AppView) {
    /*jshint jquery:true*/
    /*global Backbone:true,Store:true,_:true*/
    'use strict';

    $(function() {
        // Initialise the app and get everything going
        var app = new AppView();
    });
});